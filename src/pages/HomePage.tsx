import HomeCard from "@/components/cards/HomeCard";

const HomePage = () => {
  return (
    <main className="home--container min-h-screen h-full mx-auto flex flex-col justify-center items-center">
      <HomeCard />
    </main>
  );
};

export default HomePage;
